A simple rails consumer for https://github.com/craigedmunds/rails-api

- Once signed up and logged in to this app you can link an account from the rails API, which uses oauth-plugin
- Then makes secured calls to the API for logged in users - currently just displays the response JSON on the homepage
- Expects the Auth/API server to be running on port 3000